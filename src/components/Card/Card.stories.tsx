import * as React from 'react'
import {
  Card as NIPRCard,
  CardProps
} from "./Card";

export default {
  title: 'Components/Card',
  component: NIPRCard,
}

export const Card = {
  render: () => {
    const props = {

    }

    return <NIPRCard />
  }
}